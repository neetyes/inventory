<div class="row">
  <div class="col-md-12">
    <h3>Supplier|
    <small>List</small>
    </h3>
    <hr>
    <ol class="breadcrumb">
      <li><a href="/">Home</a></li>
      <li>Supplier</li>
    </ol>
  </div>
</div>
<div class="action text-center">
  <a href="<%= new_supplier_path %>" class="btn btn-primary">Add New Supplier</a>
</div>
<table class="table table-bordered table-hover" id="customer">
  <thead>
  <tr class="bg-success">
    <th>SN</th>
    <th>Name</th>
    <th>Address</th>
    <th>Contact no.</th>
    <th>Email</th>
    <th>Operation</th>
  </tr>
  </thead>
  <tbody>
  <% @suppliers.each_with_index do |supplier, i| %>
      <tr>
        <td>
          <%= (@page.to_i - 1) * @number_of_customers_per_page + i+1 %>
        </td>
        <td><%= supplier.name%></td>
        <td><%= supplier.address %></td>
        <td><%= supplier.contact_no %></td>
        <td><%= supplier.email %></td>
        <td>
          <%= link_to "", edit_supplier_path(supplier), :class => "fa fa-pencil", :title => "Edit" %>
          <%= link_to "", supplier_path(supplier), :class => "fa fa-trash", :title => "Delete",
                      method: :delete,
                      data: {confirm: "Are you sure"} %> <i aria-hidden="true"></i>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>
<script>
    $(document).ready(function(){
        $('#customer').DataTable();
    });
</script>